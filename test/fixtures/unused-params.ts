export function greet(name, salutation, title) {
  return title + " " + name;
}

export function ignored(_x, y) {
  return y * 2;
}

export function shorthandUser(id, label) {
  return { id, label };
}
